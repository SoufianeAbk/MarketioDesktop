<Application x:Class="Marketio_WPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Marketio_WPF">
    <Application.Resources>
        <ResourceDictionary>

            <!-- CONVERTERS staan lokaal in elke view.
                 App.xaml gebruikt alleen WPF built-in types zodat
                 de XAML designer geen project-assembly hoeft te laden. -->

            <!-- KLEURPALET  (herbruikbare brushes in het hele project) -->
            <SolidColorBrush x:Key="PrimaryBrush"       Color="#3498DB"/>
            <SolidColorBrush x:Key="SuccessBrush"       Color="#27AE60"/>
            <SolidColorBrush x:Key="DangerBrush"        Color="#E74C3C"/>
            <SolidColorBrush x:Key="WarningBrush"       Color="#F39C12"/>
            <SolidColorBrush x:Key="NeutralBrush"       Color="#95A5A6"/>
            <SolidColorBrush x:Key="DarkHeaderBrush"    Color="#2C3E50"/>
            <SolidColorBrush x:Key="BorderDefaultBrush" Color="#BDC3C7"/>
            <SolidColorBrush x:Key="AltRowBrush"        Color="#F9F9F9"/>

            <!-- BASE BUTTON-STIJL  (benoemde key – designer-veilig)
                 Stelt gedeelde eigenschappen in voor alle actieknoppen.
                 Geeft uitgeschakelde knoppen een gedimde weergave. -->
            <Style x:Key="BaseButtonStyle" TargetType="Button">
                <Setter Property="Foreground"      Value="White"/>
                <Setter Property="FontWeight"      Value="Bold"/>
                <Setter Property="Cursor"          Value="Hand"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding"         Value="10,5"/>
                <Style.Triggers>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.4"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Impliciete Button-stijl: past BaseButtonStyle automatisch toe
                 op elke Button zonder een expliciete Style-attribuut.      -->
            <Style TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}"/>

            <!-- GEKLEURDE ACTIEKNOP-STIJLEN  (BasedOn BaseButtonStyle)
                 Elke variant voegt enkel de achtergrondkleur toe. -->
            <!-- Blauw – vernieuwen, opslaan, bevestigen -->
            <Style x:Key="PrimaryButtonStyle" TargetType="Button"
                   BasedOn="{StaticResource BaseButtonStyle}">
                <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            </Style>

            <!-- Groen – aanmaken, toevoegen, toewijzen -->
            <Style x:Key="SuccessButtonStyle" TargetType="Button"
                   BasedOn="{StaticResource BaseButtonStyle}">
                <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
            </Style>

            <!-- Rood – verwijderen, gevaarlijke acties -->
            <Style x:Key="DangerButtonStyle" TargetType="Button"
                   BasedOn="{StaticResource BaseButtonStyle}">
                <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
            </Style>

            <!-- Oranje – bewerken, status wijzigen -->
            <Style x:Key="WarningButtonStyle" TargetType="Button"
                   BasedOn="{StaticResource BaseButtonStyle}">
                <Setter Property="Background" Value="{StaticResource WarningBrush}"/>
            </Style>

            <!-- Grijs – filteren, annuleren, terugknop -->
            <Style x:Key="NeutralButtonStyle" TargetType="Button"
                   BasedOn="{StaticResource BaseButtonStyle}">
                <Setter Property="Background" Value="{StaticResource NeutralBrush}"/>
            </Style>

            <!--  TEKSTBLOK-STIJLEN -->
            <!-- Paginatitel: "📦 Products", "📋 Orders", enz. -->
            <Style x:Key="ViewHeaderStyle" TargetType="TextBlock">
                <Setter Property="FontSize"          Value="22"/>
                <Setter Property="FontWeight"        Value="Bold"/>
                <Setter Property="Foreground"        Value="{StaticResource DarkHeaderBrush}"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <!-- Foutmelding (rood, klein, afbreekbaar) -->
            <Style x:Key="ErrorMessageStyle" TargetType="TextBlock">
                <Setter Property="Foreground"   Value="{StaticResource DangerBrush}"/>
                <Setter Property="FontSize"     Value="12"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
            </Style>

            <!-- Succesmelding (groen, klein, afbreekbaar) -->
            <Style x:Key="SuccessMessageStyle" TargetType="TextBlock">
                <Setter Property="Foreground"   Value="{StaticResource SuccessBrush}"/>
                <Setter Property="FontSize"     Value="12"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
            </Style>

            <!-- DATAGRID-STIJL
                 Uniforme weergave voor alle overzichtsschermen. -->
            <Style x:Key="StandardDataGridStyle" TargetType="DataGrid">
                <Setter Property="AutoGenerateColumns"      Value="False"/>
                <Setter Property="IsReadOnly"               Value="True"/>
                <Setter Property="CanUserAddRows"           Value="False"/>
                <Setter Property="CanUserDeleteRows"        Value="False"/>
                <Setter Property="SelectionMode"            Value="Single"/>
                <Setter Property="GridLinesVisibility"      Value="Horizontal"/>
                <Setter Property="HeadersVisibility"        Value="Column"/>
                <Setter Property="AlternatingRowBackground" Value="{StaticResource AltRowBrush}"/>
                <Setter Property="RowBackground"            Value="White"/>
                <Setter Property="BorderBrush"              Value="{StaticResource BorderDefaultBrush}"/>
                <Setter Property="BorderThickness"          Value="1"/>
            </Style>

            <!-- NAVIGATIEMENU  (verplaatst vanuit MainWindow.xaml) -->
            <Style x:Key="NavMenuItemStyle" TargetType="MenuItem">
                <Setter Property="Width"      Value="200"/>
                <Setter Property="Height"     Value="50"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="FontSize"   Value="14"/>
                <Setter Property="Background" Value="#34495E"/>
                <Setter Property="Padding"    Value="15,0,0,0"/>
                <Setter Property="Cursor"     Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="MenuItem">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderThickness="0"
                                    Height="{TemplateBinding Height}">
                                <ContentPresenter ContentSource="Header"
                                                  HorizontalAlignment="Left"
                                                  VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#2980B9"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#1F618D"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="NavSeparatorStyle" TargetType="Separator">
                <Setter Property="Background" Value="#2C3E50"/>
                <Setter Property="Margin"     Value="0,10,0,10"/>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>